# for create teh necessary keyspace name:
# CREATE KEYSPACE purcheaseOrderStorage WITH replication = {'class':'SimpleStrategy', 'replication_factor':1};


server.port=7070
spring.data.cassandra.keyspace-name=purcheaseOrderStorage
spring.data.cassandra.schema-action=recreate